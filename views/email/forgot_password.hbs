Reset uw wachtwoord
=====
{{# with user ~}}
<p>Hallo<strong>{{this.name}}</strong>,</p>

<p>Iemand heeft een reset aangevraagd voor uw wachtwoord op {{> email/link href=(get_application_domain) text='TimeOff.Management' }}</p>

{{# if company.ldap_auth_enabled }}
<p>Uw bedrijf gebruikt LDAP, dus kan uw wachtwoord niet gereset worden.</p>

<p>Gelieve uw administrator hierover te contacteren.</p>

{{ else }}
<p>
  Gebruik onderstaande link:

  {{> email/link href=(concatenate (get_application_domain) '/reset-password/?t=' (this.get_reset_password_token)) text='Request reset password' }}
</p>

<p>
  Ook kan uw volgende URL kopiëren en in uw browser plakken: <pre>{{ concatenate (get_application_domain) '/reset-password/?t=' (this.get_reset_password_token) }}</pre>
</p>
{{/if}}

{{~/ with}}

<p>Alvast bedankt</p>
